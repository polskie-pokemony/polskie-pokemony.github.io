import { createMuiTheme } from '@material-ui/core/styles';
{/* Sword Shield theme to be used in future*/}
const theme = createMuiTheme({
    palette: {
        primary: {
        main: '#e85397',
        },
        secondary: {
        main: '#2ecbdc',
        },
    },
});